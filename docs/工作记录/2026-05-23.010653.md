# 2026-05-23 01:06 工作记录

## 开工 01:06

- 分支: `fix/opfs-persistence-patch`
- 状态: 有未跟踪变更（`.claude/`、`README.handover.md`、`docs/`）
- 概要: 接手 duckdb-wasm OPFS DB 持久化问题，先统一消费项目验证口径，避免同 worker 内存读写造成假阳性。

### 承接上次

- 上一份日志: 无
- 已补记收工: 否
- 延续事项: 无

### 本次目标

- 在 `/Users/benz/Codes/Lesson/duckdb-wasm-web` 固化 OPFS 物理文件验证和新 worker reopen 验证。
- 把验证成功标准同步回 `docs/opfs-persistence-implementation-guide.md`。
- 验证一致后，再继续 duckdb-wasm fork 的 OPFS bridge 实现。

### 注意事项

- 父项目规则要求默认使用中文记录，包管理优先 `pnpm`。
- `keepdb-duckdb-wasm` 子项目本身沿用上游 duckdb-wasm 的 `make`/`yarn workspace` 构建脚本。
- 成功不能只看查询结果，必须同时确认 OPFS 中 `test.db` 物理文件大小大于 0。

## 进展 01:10

- 已在 `/Users/benz/Codes/Lesson/duckdb-wasm-web` 固化“完整验证”链路：清空 OPFS、写入 marker、`FORCE CHECKPOINT`、`flushFiles()`、检查 `test.db size > 0`、terminate、新 worker reopen、读回 marker、再次检查文件。
- 已执行 `pnpm run build`，构建通过。
- 已用浏览器跑完整验证，当前依赖版本按预期失败：`OPFS_PHYSICAL_FILE_EMPTY at after flushFiles: test.db size=0`，控制台出现 `Buffering missing file: opfs:/test.db`。
- 已将验证一致性标准写回 `docs/opfs-persistence-implementation-guide.md`。

## 进展 01:15

- 已开始 duckdb-wasm fork 实现：C++ `WebFileSystem` 增加 OPFS canonical path，避免 `opfs://` / `opfs:/` 注册和打开路径不一致。
- TS browser runtime 改为单 canonical key 查找，不再通过双 key 注册规避路径问题。
- TS `moveFile` 增加 `FileSystemSyncAccessHandle` copy-truncate 路径；`removeFile` 的 `_pendingDeletes` 增加 `flushFiles/dropFiles` 消费点。
- 已执行 `git diff --check`，通过。
- `pnpm --dir packages/duckdb-wasm exec tsc --noEmit` 未跑通，原因是当前 fork 未安装 `@types/emscripten`、`@types/jasmine`、`@types/node`。
